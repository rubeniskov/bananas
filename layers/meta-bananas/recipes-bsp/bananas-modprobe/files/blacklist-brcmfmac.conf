# Don't probe the on-board Broadcom 43362 WiFi — we don't ship the
# brcmfmac43362-sdio firmware blob, so brcmfmac would otherwise spam
# ~13 s of `HT Avail timeout (1000000): clkctl 0x50` errors during
# every boot. Wired gigabit Ethernet handles all BanaNAS traffic.
blacklist brcmfmac
